name: ddtrace --ini=diff repro

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      iterations:
        description: 'Iterations of `php --ini=diff` to run'
        required: false
        default: '100'
      dd_trace_release:
        description: 'ddtrace release tag (or "latest")'
        required: false
        default: 'latest'

permissions:
  contents: read

jobs:
  repro:
    name: ${{ matrix.os }} / ${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: debian
            arch: amd64
            runner: ubuntu-latest
          - os: debian
            arch: arm64
            runner: ubuntu-24.04-arm
          - os: alpine
            arch: amd64
            runner: ubuntu-latest
          - os: alpine
            arch: arm64
            runner: ubuntu-24.04-arm
    env:
      ITERATIONS: ${{ inputs.iterations || '100' }}
      DD_TRACE_RELEASE: ${{ inputs.dd_trace_release || 'latest' }}
      IMAGE: ddtrace-segv-repro:${{ matrix.os }}-${{ matrix.arch }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # GitHub Actions cache (type=gha), scoped per (os, arch) so each
      # combination keeps its own cached ddtrace install layer.
      - name: Build image (with GHA cache)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.${{ matrix.os }}
          tags: ${{ env.IMAGE }}
          load: true
          build-args: |
            DD_TRACE_RELEASE=${{ env.DD_TRACE_RELEASE }}
          cache-from: type=gha,scope=${{ matrix.os }}-${{ matrix.arch }}
          cache-to: type=gha,scope=${{ matrix.os }}-${{ matrix.arch }},mode=max

      - name: Run repro
        run: |
          docker run --rm \
            -e ITERATIONS="$ITERATIONS" \
            "$IMAGE" | tee repro-output.txt

      - name: Job summary
        if: always()
        run: |
          part1=$(grep -E '^PART1_RESULT:' repro-output.txt || echo "PART1_RESULT: <missing>")
          segv=$(grep -oE 'PART2_RESULT: SIGSEGV [0-9]+' repro-output.txt | awk '{print $3}' || echo "?")
          {
            echo "### ${{ matrix.os }} / ${{ matrix.arch }}"
            echo ""
            echo "- $part1"
            echo "- SIGSEGV: ${segv} / ${ITERATIONS}"
            echo ""
            echo "<details><summary>Full repro output</summary>"
            echo ""
            echo '```'
            cat repro-output.txt
            echo '```'
            echo ""
            echo "</details>"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload repro output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: repro-output-${{ matrix.os }}-${{ matrix.arch }}
          path: repro-output.txt
